'use strict'

let num;             // num = undefined
let age = 24;        // no need for type
console.log(age);

let f, l = "Beast";
console.log(f);
console.log(l);

// Constants
const x = 42

// vars and consts must only have [_$a-zA-Z0-9]
// vars and consts must only sart with [_$a-zA-Z]

// literal
let goal = "Hello, world";
let ref = goal

console.log(goal)
console.log(ref)

goal = "hi"

// mem independent
console.log(goal)
console.log(ref)